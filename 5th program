#Python Program for cube sum of first n natural numbers
#define a function
def nat(n):
    """function to check cube sum of first n natural numbers"""
    sum=0
    for i in range(1,n+1):
        sum +=i*i*i
    return sum

#take input from user
a=int(input(" print the number\n"))

#call the function
print(nat(a))
